<h2><%= @user.username %> has reviewed the following doctor(s): </h2>

<ul>
    
     <% @user.reviews.each do |review| %>
    <il>
        "<%= review.content %>" about <%=review.doctor.name%> <br><br>
        <%=link_to "Edit Review", edit_doctor_review_path(review.doctor, review) %><br>
        <%= link_to "Delete Review", doctor_review_path(review.doctor, review), method: 'delete'%>
    </il>
    <% end %><br><br>
</ul>



<%=link_to "List of Doctors", doctors_path %>